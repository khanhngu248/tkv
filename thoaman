
#include<iostream>
#include<conio.h>
#include<math.h>
using namespace std;
void Doan(int &a,int &b)
{
	int c;
	for(int i=a;i<=b;i++)
	if(i%3==0&&i%5==0)	
    cout<<i<<" ";
}
bool snt(int x)
{
	for(int i=2;i<=sqrt(x);i++)
	if(i%2==0)
	return fl;
	return cl;
}
int main(){
	int x,y,c;
	cin>>x>>y;
	Doan(x,y);
	snt(x);
	if(x== fl)
	{
	cout<<"\nsai";}
	if(x== cl){
	cout<<"\ndung";
	return 0;
}}
